{
  "name": "resume-zhizhen",
  "version": "1.2.0",
  "description": "简历智诊 · 求职进度管理与面试准备微信小程序（无生成式 AI，规则引擎驱动）",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run check:mp && npm run check:ai && npm run check:secrets && npm test",
    "check:mp": "node scripts/check-miniprogram.js",
    "check:ai": "node scripts/scan-ai-residual.js",
    "check:secrets": "node scripts/check-secrets.js",
    "build:functions": "node scripts/build-functions.js",
    "init:db": "node scripts/init-db.js",
    "reset:quota": "node scripts/reset-quota.js"
  },
  "devDependencies": {
    "dayjs": "^1.11.23",
    "esbuild": "^0.28.2",
    "marked": "^12.0.2",
    "tdesign-miniprogram": "^1.16.0",
    "tinycolor2": "^1.6.0",
    "tslib": "^2.8.1",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "mammoth": "^1.12.2",
    "pdf-parse": "^1.1.1",
    "wx-server-sdk": "~2.6.3"
  }
}
